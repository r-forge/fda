plot(daytempfd, ask=TRUE)
par(ask=TRUE)
plot(daytempfd)
str(gaitfd)
par("ask")
par(ask=FALSE)
par(mfrow=c(1,1))
debug(plot.fd)
str(Lfdobj
Lfdobj
class(Lfdobj)
coefd
ndim
ask
dim(fdmat)
ylabel
?create.constant.basis
basisobj <- create.constant.basis(c(-1,1))
q()
remove(list=objects())
library(fda)
gaitbasis = create.fourier.basis(0:1, 21);
gaittime <- as.numeric(dimnames(gait)[[1]]) gaitfd = data2fd(gait, gaittime, gaitbasis);
attr(gaitfd, "fdnames1") <- 'Normailzed time' #Matlab: gaitfd_fdnames{2} = 'Boys'; attr(gaitfd, "fdnames2") <- 'Boys' #Matlab: gaitfd_fdnames{3} = 'Angle (deg.)'; attr(gaitfd, "fdnames3") <- "Angle (deg.)"
harmaccelLfd = Lfd(3, wfd); # define the operator object lambda = 1e-9; fdParobj = fdPar(gaitbasis, harmaccelLfd, lambda);
Lbasisobj = create.constant.basis(0:1); #Matlab: Lcoef = [0, (2*pi)^2, 0]; Lcoef = c(0, (2*pi)^2, 0); #Matlab: wfd = fd(Lcoef, Lbasisobj); wfd = fd(t(Lcoef), Lbasisobj);
harmaccelLfd = Lfd(3, wfd); # define the operator object
lambda = 1e-9; fdParobj = fdPar(gaitbasis, harmaccelLfd, lambda);
gaitfd = smooth.fd(gaitfd, fdParobj);
plot(gaitfd)
plot(gaitfd, ask=TRUE)
q()
